import type { VideoProvider } from "../../shared/types";

/**
 * Provider abstraction surface.
 * Current implementation: BytePlusSeedanceProvider only.
 * Future: HiggsfieldProvider, MuApiProvider, KlingProvider — not in MVP.
 */
export type { VideoProvider };
